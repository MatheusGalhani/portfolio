import React from 'react';
import Header from '../../../components/header/header';
import { Container } from './home.styled';

const Home: React.FC = () => {
    return (
        <Container>
            <Header />
        </Container>
    );
};

export default Home;
